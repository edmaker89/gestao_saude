apiVersion: v1
kind: Service
metadata:
  name: gestao-saude-app
  labels:
    app: gestao-saude-app
spec:
  type: ClusterIP
  selector:
    app: gestao-saude-app
  ports:
    - name: http
      port: 80
      targetPort: http
